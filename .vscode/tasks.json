{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile & Run",
      "type": "shell",
      "command": "cmd",
      "args": [
        "/c",
        "if not exist \"${workspaceFolder}\\output\" mkdir \"${workspaceFolder}\\output\" && taskkill /f /im recursion.exe >nul 2>&1 & g++ \"${file}\" -o \"${workspaceFolder}\\output\\recursion.exe\" && \"${workspaceFolder}\\output\\recursion.exe\""
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      },
      "problemMatcher": "$gcc"
    }
  ]
}